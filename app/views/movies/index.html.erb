<h1>all movies youve watched</h1>
<div class="row">
  <% @movies.uniq.each do |movie| %>
    <div class="result-cards">
      <%= link_to image_tag(movie.poster), movie_path(movie) %>
    </div>
  <% end %>
</div>
